@model IEnumerable<Project_Final_ITI.Models.Course>

@{
    ViewData["Title"] = "Courses";
}


<h2>All Courses</h2>

<p>
    <a asp-action="Create" asp-controller="Course" class="btn btn-success">+ Create New Course</a>
</p>


<form asp-action="Index" asp-controller="Course" method="get" class="mb-3">
    <div class="input-group">
        <input type="text" name="searchString" value="@Context.Request.Query["searchString"]"
               class="form-control" placeholder="Search by Course Name or Category..." />
        <button type="submit" class="btn btn-primary">Search</button>
    </div>
</form>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Course Name</th>
            <th>Category</th>
            <th>Instructor</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model)
        {
            <tr>
                <td>@course.CourseName</td>
                <td>@course.Category</td>
                <td>@course.InstructorID</td>
                <td><a class="btn btn-success" asp-controller="Course" asp-action="Edit" asp-route-id="@course.CourseId">Edit</a></td>
                <td>
                    <form asp-action="Delete" asp-controller="Course" asp-route-id="@course.CourseId" method="post">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
